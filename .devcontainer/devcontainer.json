{
    "image": "temporalio/gitpod-edu:1.4",
    "customizations": {
        "codespaces": {
            "openFiles": [
                "README.md"
            ]
        }
    },
    "forwardPorts": [8080, 7233, 9999],
    "postStartCommand": "bash -c 'python -m venv .env && source .env/bin/activate && python -m pip install -r requirements.txt && temporal server start-dev --ui-port 8080'",
    "postAttachCommand": "source .env/bin/activate",
    "remoteEnv": {
        "PROMPT_DIRTRIM": "2"
    }
}
